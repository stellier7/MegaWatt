# Product photos

Drop product images here, then set the matching `image` path on each product in `assets/js/products.js`.

Example:

```js
image: 'assets/images/foco-led-6w.jpg'
```

Until a path is set, the catalog shows a “Foto pendiente” placeholder.
